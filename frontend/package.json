{
  "name": "frontend",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --load --env .env.dev",
    "start": "astro dev --load --env .env.dev",
    "build": "astro build --load --env .env.prod",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "@astrojs/tailwind": "^4.0.0",
    "@fontsource/raleway": "^5.0.3",
    "astro": "^2.6.5",
    "tailwindcss": "^3.3.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "prettier": "^2.8.8",
    "prettier-plugin-astro": "^0.10.0"
  }
}
