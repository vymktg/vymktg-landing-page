---
import Logo from "./Logo.astro";
import NavbarItem from "./NavbarItem.astro";
import MenuCollapseButton from "./MenuCollapseButton.astro";
import CallNowButton from "./CallNowButton.astro";
import NavigationCollapse from "./NavigationCollapse.astro";
import { NAVIGATION_ITEM } from "../../config";
---

<nav class="absolute inset-x-0 top-0 z-50 py-6">
  <div
    class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto px-4"
  >
    <MenuCollapseButton className={"hidden md:block lg:hidden"} />
    <Logo />
    <ul
      class="hidden lg:flex flex-row font-medium rounded-lg"
      data-href={Astro.url.pathname}
    >
      {
        NAVIGATION_ITEM.map((item) => (
          <NavbarItem
            href={item.href}
            title={item.title}
            isActiveUrl={item.href === Astro.url.pathname}
          />
        ))
      }
    </ul>

    <MenuCollapseButton className={"sm:block md:hidden"} />
    <CallNowButton />
    <NavigationCollapse />
  </div>

  <div
    id="drawer-backdrop"
    class="bg-gray-900 bg-opacity-50 fixed inset-0 z-30 hidden"
  >
  </div>
</nav>
