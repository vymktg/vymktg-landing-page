---
export interface Props {
  title: string;
  href: string;
}

const { title, href } = Astro.props;

let className = "duration-300 font-medium ease-linear py-2 px-4 ";

className +=
  href === Astro.url.pathname
    ? "text-primary border-b-primary border-b-2"
    : "rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-primary";
---

<a href={href} class={className} aria-current="page">{title}</a>
